import { Link, LinkShape, Rect } from "../core/types"


const generatePath = (from: Rect, to: Rect, shape: LinkShape): string => {
  return ""
}

const LinkComp: React.FC<Link> = () => {
  return <div></div>
}

export default LinkComp